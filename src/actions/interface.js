import * as types from '../types/interface';

export const switchBetCreator = (
) => ({
  type: types.SWITCH_BET_CREATOR,
});

export const resetSubmittedStatus = () => ({
  type: types.SUBMITTED_STATUS_RESETED,
});
