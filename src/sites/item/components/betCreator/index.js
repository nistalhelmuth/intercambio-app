import React, { Component } from 'react';
import './styles.css';



const BetCreator = ({ img, stars, description, date}) => (
  <div className="betCreator">
    {'nueva'}
    
  </div>
);

export default BetCreator;